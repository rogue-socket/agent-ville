[project]
name = "agent-ville"
version = "0.1.0"
description = "A village of evolving AI agents — watch them grow, die, and evolve"
requires-python = ">=3.11"

[project.scripts]
agent-ville = "agent_ville.__main__:main"
